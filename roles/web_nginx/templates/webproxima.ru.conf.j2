server {

    listen 80;

    server_name {{ item.name }}
    root {{ item.root }};
    index index.html

    server_tokens off;
    charset utf-8;

    location / {
        try_files $uri =404

        }

    }
